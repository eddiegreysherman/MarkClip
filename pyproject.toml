[project]
name = "mark-clip"
version = "0.1.0"
description = "A simple Markdown editor with live preview and rich text copy"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.6.0",
    "mistune>=2.0.0",
]

[project.scripts]
mark-clip = "main:main"
